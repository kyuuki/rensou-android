<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <RelativeLayout
        android:id="@+id/latestKeywordLayout"
        style="@style/LatestKeywordLayoutStyle"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:padding="16dp">

        <TextView
            android:id="@+id/latestKeywordText"
            style="@style/LatestKeywordTextStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text=""
            android:textColor="@color/theme300"
            android:textSize="36sp" />
    </RelativeLayout>

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/latestKeywordLayout"
        android:layout_centerHorizontal="true"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/postRensouEditText"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_weight="1"
            android:background="@color/grey200"
            android:ellipsize="end"
            android:hint="@string/post_rensou_hist_input_rensou"
            android:inputType="text"
            android:maxLength="13"
            android:maxLines="1"
            android:padding="5dp"
            android:singleLine="true"
            android:textSize="20sp" />

        <Button
            android:id="@+id/postButton"
            style="@style/PostButtonStyle"
            android:layout_marginLeft="5dp" />

    </LinearLayout>

</RelativeLayout>