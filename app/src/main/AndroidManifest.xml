<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.kyuuki.rensou.android"
    android:versionCode="12"
    android:versionName="1.1.3" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- GCM -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <permission android:name="jp.kyuuki.rensou.android.development.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />
    <user-permission android:name="jp.kyuuki.rensou.android.development.permission.C2D_MESSAGE" />

    <application
        android:name="AnalyticsApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/U1Theme" >
        <activity
            android:name=".activities.RoomActivity"
            android:label="@string/app_name"
            android:theme="@style/NormalActivityTheme">
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/NormalActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.PostResultActivity"
            android:label="@string/activity_result_title"
            android:theme="@style/NormalActivityTheme">
        </activity>
        <activity
            android:name=".activities.RankingActivity"
            android:label="@string/activity_ranking_title"
            android:theme="@style/NormalActivityTheme">
        </activity>
        <activity
            android:name=".activities.AboutActivity"
            android:theme="@style/NormalActivityTheme">
        </activity>
        
        <!--  AdMob Ads SDK -->
        <activity android:name="com.google.ads.AdActivity"
              android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />

        <!-- GCM -->
        <receiver
            android:name="com.google.android.gms.gcm.GcmReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <category android:name="com.example.gcm" />
            </intent-filter>
        </receiver>
        <service
            android:name="jp.kyuuki.rensou.android.services.RensouGcmListenerService"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>
        <service
            android:name="jp.kyuuki.rensou.android.services.RegistrationIntentService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.android.gms.iid.InstanceID" />
            </intent-filter>
        </service>

    </application>

</manifest>
